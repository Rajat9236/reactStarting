/* eslint-disable */

import React from 'react';
import styled from 'styled-components';

const MainDiv = styled.div`
margin: 0px;
background: ${props => (props.chatting ? '#fbfbfb' : 'transparent' )}


`;

export default MainDiv;
